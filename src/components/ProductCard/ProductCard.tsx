interface ProductCardProps {
  image: string;
  title: string;
  description: string;
  price: number;
}

const ProductCard = ({
  image,
  title,
  description,
  price,
}: ProductCardProps) => {
  return <></>;
};

export default ProductCard;
